# Results kept on this branch

`lazylean-2026-09-22.json` is the `_build/results.json` written by `lka.py write-results`
after `lka.py build-test` (all 79 test definitions) and `lka.py run --checker` for
`lazylean`, `official`, `eink0rn` and `nanoclo`, on one rented machine (AMD EPYC 7B13, 64
threads, 251 GB), 22 September 2026, at commit 3534189 of this branch. The other 22 checkers
in the file carry no results from this run. Memory (`max_rss`) is zero throughout: the
runner could not measure it on that machine.
